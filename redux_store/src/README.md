## HOW TO USE REDUX STORE

A. INSTALL @reduxjs/toolkit and redux-store
1.We need to create store by using configureStore({}) and configure store contains reducer
2.we need to define the reducer/slice for that we have createSlice({})
createSlice needs 3 things, I. name II. initialState III. reducer
3.once createSlice is done.
we need to export actions and slice (createSlice).
4.we need to import created reducers to store.

## Refer the document which is already create
